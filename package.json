{
  "name": "ci-cd-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && echo '<h1>Enterprise Build Successful!</h1>' > dist/index.html",
    "test": "echo 'Running unit tests...' && exit 0"
  }
}
